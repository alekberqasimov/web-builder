import {findNode,findNodeWithParent,moveNode} from './v5-model.mjs';
import {$,$$,state,currentPage,mutate} from './v5-runtime.mjs';

let bound=false,mouseDrag=null,touchDrag=null,touchTimer=null,styleReady=false,suppressClickUntil=0,lastPlanReason='';
const excluded=t=>!!t.closest?.('.v5-block-toolbar,.v5-inline-add,input,textarea,select,button');
const debug=(phase,extra={})=>{window.__v5ElementDnD={...(window.__v5ElementDnD||{}),phase,time:Date.now(),...extra}};
function ensureStyle(){if(styleReady)return;styleReady=true;const s=document.createElement('style');s.textContent=`
#canvas .v5-container[data-node-id]{min-height:44px}
#canvas .v5-node.pointer-element-source{opacity:.62;cursor:grabbing!important;user-select:none!important}
#canvas .v5-node.pointer-element-source *{cursor:grabbing!important;user-select:none!important}
#canvas .pointer-element-drop.drop-target{outline:2px solid #705cff!important;outline-offset:3px}
#canvas .pointer-element-drop.drop-before{box-shadow:0 -3px 0 #705cff!important}
#canvas .pointer-element-drop.drop-after{box-shadow:0 3px 0 #705cff!important}
`;document.head.appendChild(s)}
function wrap(){return $('.canvas-wrap')}
function blockById(id){return currentPage().blocks.find(b=>b.id===id)}
function clearMarks(){$$('#canvas .pointer-element-drop').forEach(x=>x.classList.remove('pointer-element-drop','drop-target','drop-before','drop-after'))}
function clearTimer(){if(touchTimer){clearTimeout(touchTimer);touchTimer=null}}
function restoreSource(d){if(!d?.source)return;d.source.classList.remove('pointer-element-source');d.source.draggable=d.wasDraggable}
function resetTouch(){clearTimer();clearMarks();restoreSource(touchDrag);touchDrag=null}
function autoScroll(y){const w=wrap();if(!w)return;const r=w.getBoundingClientRect(),edge=72,max=22;if(y<r.top+edge)w.scrollTop-=Math.ceil(max*(1-(y-r.top)/edge));else if(y>r.bottom-edge)w.scrollTop+=Math.ceil(max*(1-(r.bottom-y)/edge))}
export function pointerElementZone(y,top,height,edgeRatio=.22){const edge=Math.min(28,Math.max(10,height*edgeRatio));if(y<=top+edge)return'before';if(y>=top+height-edge)return'after';return'inside'}
function candidateNodeFromPoint(d,x,y){const stack=document.elementsFromPoint(x,y);for(const el of stack){const n=el.closest?.('#canvas [data-node-id]');if(!n)continue;if(n===d.source||d.source.contains(n))continue;lastPlanReason='';return n}const containers=$$('#canvas .v5-container[data-node-id]').filter(n=>n!==d.source&&!d.source.contains(n)&&n.offsetParent!==null);let best=null,bestDist=Infinity;for(const n of containers){const r=n.getBoundingClientRect(),dx=x<r.left?r.left-x:x>r.right?x-r.right:0,dy=y<r.top?r.top-y:y>r.bottom?y-r.bottom:0,dist=Math.hypot(dx,dy);if(dist<bestDist&&dist<=36){best=n;bestDist=dist}}if(best){lastPlanReason='nearest-container-fallback';return best}lastPlanReason=`no-live-node@${Math.round(x)},${Math.round(y)}:${stack.slice(0,4).map(el=>`${el.tagName}.${String(el.className||'').split(' ').slice(0,2).join('.')}`).join('|')}`;return null}
function planAt(d,x,y){lastPlanReason='';const live=candidateNodeFromPoint(d,x,y);if(!live)return null;const sec=live.closest('[data-block-id]'),block=blockById(sec?.dataset.blockId),target=block&&findNode(block.root,live.dataset.nodeId);if(!block){lastPlanReason='target-block-not-found';return null}if(!target){lastPlanReason=`target-node-not-found:${live.dataset.nodeId||''}`;return null}const srcBlock=blockById(d.blockId),srcNode=srcBlock&&findNode(srcBlock.root,d.nodeId);if(!srcBlock||!srcNode){lastPlanReason='source-model-not-found';return null}if(srcBlock===block&&(target.id===srcNode.id||findNode(srcNode,target.id))){lastPlanReason='self-or-descendant';return null}const r=live.getBoundingClientRect();if(target.type==='container'){const info=findNodeWithParent(block.root,target.id),zone=info?.parent?.type==='container'?pointerElementZone(y,r.top,r.height):'inside';if(zone==='inside'){if(target.locked){lastPlanReason='target-locked';return null}return{blockId:block.id,containerId:target.id,beforeId:'',zone,live}}const parent=info?.parent;if(parent?.type!=='container'){lastPlanReason='container-parent-not-container';return null}if(parent.locked){lastPlanReason='container-parent-locked';return null}const siblings=parent.children,idx=siblings.findIndex(n=>n.id===target.id),beforeId=zone==='before'?target.id:(siblings[idx+1]?.id||'');return{blockId:block.id,containerId:parent.id,beforeId,zone,live}}const info=findNodeWithParent(block.root,target.id);if(info?.parent?.type!=='container'){lastPlanReason='element-parent-not-container';return null}if(info.parent.locked){lastPlanReason='element-parent-locked';return null}const zone=y<r.top+r.height/2?'before':'after',siblings=info.parent.children,idx=siblings.findIndex(n=>n.id===target.id),beforeId=zone==='before'?target.id:(siblings[idx+1]?.id||'');return{blockId:block.id,containerId:info.parent.id,beforeId,zone,live}}
function mark(plan){clearMarks();if(!plan)return;plan.live.classList.add('pointer-element-drop',plan.zone==='inside'?'drop-target':plan.zone==='before'?'drop-before':'drop-after')}
function baseDrag(node,x,y,pointerType){const sec=node.closest('[data-block-id]'),block=blockById(sec?.dataset.blockId);if(!block){debug('rejected',{reason:'block-not-found',nodeId:node.dataset.nodeId||''});return null}if(node.dataset.locked==='1'){debug('rejected',{reason:'locked',nodeId:node.dataset.nodeId||''});return null}const info=findNodeWithParent(block.root,node.dataset.nodeId);if(!info?.parent){debug('rejected',{reason:'parent-not-found',nodeId:node.dataset.nodeId||'',blockId:block.id});return null}return{pointerType,blockId:block.id,nodeId:node.dataset.nodeId,startX:x,startY:y,source:node,wasDraggable:node.draggable,active:false,plan:null}}
function activate(d){if(!d||d.active)return;d.active=true;d.source.classList.add('pointer-element-source');d.source.draggable=false;debug('activated',{nodeId:d.nodeId,blockId:d.blockId,reason:''})}
function moveAcross(srcBlockId,nodeId,targetBlockId,targetContainerId,beforeId=''){let moved=false;mutate('Move element',()=>{const src=blockById(srcBlockId),dst=blockById(targetBlockId);if(!src||!dst)return;if(src===dst){moved=moveNode(src,nodeId,targetContainerId,beforeId);if(!moved)return}else{const found=findNodeWithParent(src.root,nodeId),target=findNode(dst.root,targetContainerId);if(!found?.parent||target?.type!=='container'||target.locked)return;found.parent.children=found.parent.children.filter(n=>n.id!==nodeId);const idx=beforeId?target.children.findIndex(n=>n.id===beforeId):-1;idx>=0?target.children.splice(idx,0,found.node):target.children.push(found.node);moved=true}if(moved){state.selectedBlockId=dst.id;state.selectedNodeId=nodeId;state.multi.clear();state.multi.add(nodeId);state.activeRight='element'}});debug(moved?'committed':'commit-failed',{nodeId,targetBlockId,targetContainerId,beforeId});return moved}
function commitDrag(d){if(!d?.active||!d.plan)return false;const p=d.plan,moved=moveAcross(d.blockId,d.nodeId,p.blockId,p.containerId,p.beforeId);if(moved)suppressClickUntil=Date.now()+350;return moved}
function mouseDown(e){debug('mousedown-seen',{target:e.target?.tagName||'',button:e.button,reason:''});if(e.button!==0||touchDrag){debug('mousedown-rejected',{reason:e.button!==0?'button':'touch-active'});return}const node=e.target.closest?.('#canvas [data-node-id]');if(!node){debug('mousedown-rejected',{reason:'no-node'});return}if(e.target.closest?.('.v5-block-drag')){debug('mousedown-rejected',{reason:'block-handle'});return}if(excluded(e.target)){debug('mousedown-rejected',{reason:'excluded',target:e.target?.tagName||''});return}mouseDrag=baseDrag(node,e.clientX,e.clientY,'mouse');if(!mouseDrag)return;mouseDrag.source.draggable=false;debug('armed',{nodeId:mouseDrag.nodeId,blockId:mouseDrag.blockId,target:e.target?.tagName||'',reason:''})}
function updatePlan(d,e){const next=planAt(d,e.clientX,e.clientY);d.plan=next;debug(next?'planned':'no-plan',{nodeId:d.nodeId,x:e.clientX,y:e.clientY,targetBlockId:next?.blockId||'',targetContainerId:next?.containerId||'',zone:next?.zone||'',reason:next?'':lastPlanReason});mark(next);autoScroll(e.clientY)}
function mouseMove(e){if(!mouseDrag)return;const d=mouseDrag,dist=Math.hypot(e.clientX-d.startX,e.clientY-d.startY);if(!d.active){if(dist<5)return;activate(d)}e.preventDefault();e.stopPropagation();updatePlan(d,e)}
function mouseUp(e){const d=mouseDrag;if(!d)return;const shouldCommit=d.active&&d.plan;clearMarks();restoreSource(d);mouseDrag=null;if(shouldCommit)commitDrag(d);else debug('mouseup-no-commit',{active:d.active,hasPlan:!!d.plan,reason:lastPlanReason});if(d.active){e.preventDefault();e.stopPropagation()}}
function touchDown(e){if(e.pointerType==='mouse'||mouseDrag)return;const node=e.target.closest?.('#canvas [data-node-id]');if(!node||e.target.closest?.('.v5-block-drag')||excluded(e.target))return;touchDrag=baseDrag(node,e.clientX,e.clientY,e.pointerType||'touch');if(!touchDrag)return;e.stopPropagation();touchTimer=setTimeout(()=>{const d=touchDrag;if(!d||d.pointerId&&d.pointerId!==e.pointerId)return;activate(d);try{d.source.setPointerCapture(e.pointerId)}catch{}},360);touchDrag.pointerId=e.pointerId}
function touchMove(e){const d=touchDrag;if(!d||e.pointerId!==d.pointerId)return;const dist=Math.hypot(e.clientX-d.startX,e.clientY-d.startY);if(!d.active){if(dist>10)resetTouch();return}e.preventDefault();e.stopPropagation();updatePlan(d,e)}
function touchEnd(e,cancel=false){const d=touchDrag;if(!d||e.pointerId!==d.pointerId)return;clearTimer();try{d.source.releasePointerCapture(e.pointerId)}catch{}const shouldCommit=d.active&&!cancel&&d.plan;clearMarks();restoreSource(d);touchDrag=null;if(shouldCommit)commitDrag(d);if(d.active){e.preventDefault();e.stopPropagation()}}
export function bindPointerElementDnD(){if(bound)return;bound=true;ensureStyle();debug('bound',{reason:''});document.addEventListener('mousedown',mouseDown,true);document.addEventListener('mousemove',mouseMove,{capture:true,passive:false});document.addEventListener('mouseup',mouseUp,true);document.addEventListener('pointerdown',touchDown,true);document.addEventListener('pointermove',touchMove,{capture:true,passive:false});document.addEventListener('pointerup',e=>touchEnd(e,false),true);document.addEventListener('pointercancel',e=>touchEnd(e,true),true);document.addEventListener('dragstart',e=>{if(e.target.closest?.('#canvas [data-node-id]')){e.preventDefault();e.stopImmediatePropagation()}},true);document.addEventListener('click',e=>{if(Date.now()<suppressClickUntil&&e.target.closest?.('#canvas [data-node-id]')){e.preventDefault();e.stopImmediatePropagation()}},true)}
