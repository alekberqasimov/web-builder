import {$,$$,state} from './v5-runtime.mjs';

let vx=null,hy=null,badge=null;
function ensure(){if(vx)return;vx=document.createElement('div');hy=document.createElement('div');badge=document.createElement('div');vx.className='v5-smart-guide vertical';hy.className='v5-smart-guide horizontal';badge.className='v5-smart-guide-badge';document.body.append(vx,hy,badge);const s=document.createElement('style');s.textContent=`.v5-smart-guide{position:fixed;z-index:20000;pointer-events:none;background:#ff4fa3;display:none;box-shadow:0 0 0 1px rgba(255,255,255,.35)}.v5-smart-guide.vertical{top:82px;bottom:0;width:1px}.v5-smart-guide.horizontal{left:0;right:0;height:1px}.v5-smart-guide.show{display:block}.v5-smart-guide-badge{position:fixed;z-index:20001;display:none;pointer-events:none;background:#ff4fa3;color:#fff;border-radius:999px;padding:3px 7px;font:700 10px/1.2 Inter,Arial,sans-serif}.v5-smart-guide-badge.show{display:block}`;document.head.appendChild(s)}
function hide(){vx?.classList.remove('show');hy?.classList.remove('show');badge?.classList.remove('show')}
export function nearestGuide(candidates,pos,tolerance=8){let best=null,dist=Infinity;for(const c of candidates||[]){const d=Math.abs(Number(c.value)-Number(pos));if(d<dist&&d<=tolerance){best={...c,dist:d};dist=d}}return best}
export function guideCandidates(rects=[],canvasRect=null){const xs=[],ys=[];for(const r of rects){xs.push({value:r.left,label:'LEFT'},{value:r.left+r.width/2,label:'CENTER X'},{value:r.right,label:'RIGHT'});ys.push({value:r.top,label:'TOP'},{value:r.top+r.height/2,label:'CENTER Y'},{value:r.bottom,label:'BOTTOM'})}if(canvasRect){xs.push({value:canvasRect.left,label:'CANVAS LEFT'},{value:canvasRect.left+canvasRect.width/2,label:'CANVAS CENTER'},{value:canvasRect.right,label:'CANVAS RIGHT'})}return{xs,ys}}
function sourceId(){return state.drag?.nodeId||''}
export function bindSmartGuides(){if(document.body.dataset.v5SmartGuides==='1')return;document.body.dataset.v5SmartGuides='1';ensure();document.addEventListener('dragover',e=>{if(!state.drag||!['node','new-element'].includes(state.drag.kind)||!e.target.closest?.('#canvas')){hide();return}const canvas=$('#canvas'),cr=canvas?.getBoundingClientRect(),sid=sourceId(),nodes=$$('#canvas [data-node-id]').filter(x=>x.dataset.nodeId!==sid&&x.offsetParent!==null),rects=nodes.map(el=>el.getBoundingClientRect()),c=guideCandidates(rects,cr),x=nearestGuide(c.xs,e.clientX),y=nearestGuide(c.ys,e.clientY);if(x){vx.style.left=`${Math.round(x.value)}px`;vx.classList.add('show')}else vx.classList.remove('show');if(y){hy.style.top=`${Math.round(y.value)}px`;hy.classList.add('show')}else hy.classList.remove('show');if(x||y){badge.textContent=[x?.label,y?.label].filter(Boolean).join(' · ');badge.style.left=`${Math.min(innerWidth-150,e.clientX+10)}px`;badge.style.top=`${Math.max(88,e.clientY-24)}px`;badge.classList.add('show')}else badge.classList.remove('show')},true);document.addEventListener('drop',hide,true);document.addEventListener('dragend',hide,true);document.addEventListener('pointerup',hide,true)}
